import { StyleSheet} from 'react-native';
import colors from './colors'

export default StyleSheet.create({

    container: {
      backgroundColor: "#888",
      flex: 1,
      alignItems: 'center',
   },
    title: {
        fontSize: 22,
        fontWeight: 'bold',
        color: colors.color1,
    }
  });